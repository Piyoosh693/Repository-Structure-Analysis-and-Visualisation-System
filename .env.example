# Frontend Environment Configuration
# Used by Vite via import.meta.env

# API Backend URL
# Default: empty, which uses Vite's same-origin /api proxy in development.
# For production/custom setup, set this environment variable when building.
# Example: VITE_API_URL=https://api.example.com npm run build
# VITE_API_URL=http://localhost:8000
